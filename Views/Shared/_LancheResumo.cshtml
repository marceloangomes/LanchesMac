@model Lanche
@{
    ViewData["Title"] = "_LancheResumo";
    Layout = null;
}

 <div class="col-sm-4 col-md-4 col-lg-4">
    <p><img class="img-fluid" src="~/@Model.imagemUrl"></p>
    <h3>Preço: @Model.Preco.ToString("c")</h3>
    <h4>
        <a asp-controller="Lanche" asp-action="Details" 
            asp-route-Id="@Model.Id">@Model.Nome</a>
    </h4>
    <h4>Lanche: @Model.Nome</h4>
    <p>@Model.DescricaoCurta</p>
    <div>
        <p class="button">
            <a class="btn btn-primary" id="cartButton" asp-controller="CarrinhoCompra" 
            asp-action="Adicionar" asp-route-Id="@Model.Id">
            Incluir no Carrinho
            </a>
        </p>
    </div>

</div>
